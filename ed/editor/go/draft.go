package main

import "fmt"

func main() {
	var input string
	fmt.Scanln(&input)

	for i := 0; i < len(input); i++ {

	}

	for i := 0; i < len(input); i++ {
		char := input[i]
		switch char {
		case 'R':
			fmt.Println()

		}
	}
}
